version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: lh-gateway-redis
    ports:
      - "16379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
