# Gateway Admin UI

React + Vite 管理端，提供运行概览、Provider CRUD、启停控制和调用日志查看。

开发环境默认通过 Vite 将 `/api` 代理到 `http://localhost:8081`；也可设置
`VITE_ADMIN_API_BASE` 指向其他管理 API 地址。

```bash
npm install
npm run dev
```
